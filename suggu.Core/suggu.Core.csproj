<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <Folder Include="Generation\" />
    <Folder Include="Inspection\" />
  </ItemGroup>

  <!-- The default pack ships inside suggu.Core; the loader reads it by resource name. -->
  <ItemGroup>
    <EmbeddedResource Include="Packs\Default\**\*" />
  </ItemGroup>

</Project>
